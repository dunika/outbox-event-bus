{
  "name": "outbox-event-bus/example-mongo-outbox",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "docker-compose up -d --wait && vitest run src/e2e.test.ts; status=$?; docker-compose down; exit $status"
  },
  "types": "./src/index.ts",
  "dependencies": {
    "outbox-event-bus": "file:../..",
    "mongodb": "^6.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
