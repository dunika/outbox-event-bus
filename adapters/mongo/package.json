{
  "name": "@outbox-event-bus/mongo-outbox",
  "version": "0.0.1",
  "private": false,
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run -c vitest.config.ts",
    "test:e2e": "docker-compose up -d --wait && vitest run src/e2e.test.ts; status=$?; docker-compose down; exit $status"
  },
  "types": "./src/index.ts",
  "dependencies": {
    "mongodb": "^7.0.0",
    "outbox-event-bus": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
