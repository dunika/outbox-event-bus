{
  "name": "@outbox-event-bus/dynamodb-outbox",
  "version": "0.0.1",
  "private": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.956.0",
    "@aws-sdk/lib-dynamodb": "^3.956.0",
    "outbox-event-bus": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "testcontainers": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "scripts": {
    "test": "vitest run src/dynamodb-outbox.test.ts",
    "test:e2e": "docker-compose up -d --wait && vitest run src/e2e.test.ts; status=$?; docker-compose down; exit $status"
  }
}
