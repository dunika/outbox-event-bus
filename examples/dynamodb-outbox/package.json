{
  "name": "outbox-event-bus/example-dynamodb-outbox",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "outbox-event-bus": "file:../..",
    "@aws-sdk/client-dynamodb": "^3.712.0",
    "@aws-sdk/lib-dynamodb": "^3.712.0"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^4.0.16",
    "testcontainers": "^10.2.1",
    "@types/node": "^25.0.3"
  },
  "scripts": {
    "test": "vitest run src/index.test.ts",
    "test:e2e": "docker-compose up -d --wait && vitest run src/e2e.test.ts; status=$?; docker-compose down; exit $status"
  }
}
