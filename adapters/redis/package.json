{
  "name": "@outbox-event-bus/redis-outbox",
  "version": "0.0.1",
  "private": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "ioredis": "catalog:",
    "outbox-event-bus": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "ioredis-mock": "^8.13.1",
    "testcontainers": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "scripts": {
    "test": "vitest run src/redis-outbox.test.ts",
    "test:e2e": "docker-compose up -d --wait && vitest run src/e2e.test.ts; status=$?; docker-compose down; exit $status"
  }
}
